services:
  itco-dashboard:
    build: .
    container_name: itco-dashboard
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - DATABASE_PATH=/app/data/dashboard.db
      - PYTHONUNBUFFERED=1
    volumes:
      - ./data:/app/data
